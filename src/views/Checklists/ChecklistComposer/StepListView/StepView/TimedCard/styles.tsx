import styled from 'styled-components';

export const Wrapper = styled.div`
  margin: 24px 40px 0;
  border: 1px dashed #bababa;
  border-radius: 5px;
  padding: 16px;

  .timed-rule {
    display: flex;
  }
`;
