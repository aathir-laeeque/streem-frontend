import React, { FC } from 'react';
import { RouteComponentProps } from '@reach/router';

const ListView: FC<RouteComponentProps> = () => (
  <div>this is the checklist list view</div>
);

export default ListView;
